# Запуск WFRP Game Master Bot в Docker

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Предварительные требования

```bash
# Проверьте установку Docker
docker --version
docker-compose --version
```

### Шаг 2: Подготовка конфигурации

```bash
# Копируем пример конфигурации Minimax
cp .env.minimax.example .env

# Редактируем файл конфигурации
nano .env
# или
vim .env
# или
open .env  # macOS
```

### Шаг 3: Заполнение .env

Обязательные поля:
```env
TELEGRAM_BOT_TOKEN=your_actual_token_here
TELEGRAM_GROUP_ID=-1001234567890
MINIMAX_API_KEY=your_minimax_api_key_here
```

Как получить значения:
- **Telegram токен**: @BotFather → /newbot
- **Group ID**: @userinfobot → перешлите бота в группу
- **Minimax API**: https://api.minimax.chat/

### Шаг 4: Запуск контейнера

```bash
# Вариант 1: Запуск в detached режиме (в фоне)
docker-compose up -d

# Вариант 2: Запуск с логами в терминале
docker-compose up

# Вариант 3: Построить заново
docker-compose up --build -d
```

### Шаг 5: Проверка работоспособности

```bash
# Просмотр логов контейнера
docker-compose logs -f

# Проверка статуса контейнера
docker-compose ps

# Остановить контейнер
docker-compose stop

# Запустить снова
docker-compose start

# Полная перезагрузка
docker-compose restart
```

---

## 🧪 Тестирование

### Тест 1: Проверка запуска
```bash
# Ждем 10 секунд после запуска
sleep 10

# Проверяем логи
docker-compose logs --tail=50
```

Ожидаемый вывод:
```
2026/02/17 13:30:00 WFRP Game Master Bot
2026/02/17 13:30:00 Starting bot...
2026/02/17 13:30:00 Config loaded. Provider: minimax, Group ID: -1001234567890
```

### Тест 2: Проверка монтирования директорий

```bash
# Входим в контейнер
docker-compose exec wfrp-bot sh

# Проверяем файлы
ls -la /app/rules/
ls -la /app/history/
cat /app/.env

# Выходим
exit
```

### Тест 3: Тестирование бота через Telegram

1. Откройте вашу группу Telegram
2. Найдите бота в списке участников
3. Попробуйте команды:
   - `/start` - начать игру
   - `/help` - справка по командам
   - `/status` - статус сессии

### Тест 4: Тестирование Minimax провайдера

```bash
# Мониторинг логов в реальном времени
docker-compose logs -f wfrp-bot
```

В логах должны появляться:
- При успешном запросе к Minimax
- При ошибках API Minimax

---

## 🛠️ Управление контейнером

### Основные команды:

```bash
# Статус контейнера
docker-compose ps

# Просмотр логов
docker-compose logs [service_name]
docker-compose logs -f [service_name]
docker-compose logs --tail=100 [service_name]

# Остановка
docker-compose stop

# Запуск
docker-compose start

# Перезапуск
docker-compose restart

# Удаление и пересоздание
docker-compose down
docker-compose up -d

# Удаление данных контейнера
docker-compose down -v
```

### Взаимодействие с контейнером:

```bash
# Вход в контейнер
docker-compose exec wfrp-bot sh

# Выполнение команд внутри контейнера
docker-compose exec wfrp-bot ls -la /app/

# Просмотр переменных окружения
docker-compose exec wfrp-bot env | grep TELEGRAM

# Перезагрузка конфигурации (отправка SIGHUP)
docker-compose exec wfrp-bot kill -HUP 1

# Проверка здоровья контейнера
docker-compose ps
```

---

## 🔧 Ручная отладка

### Если контейнер не запускается:

1. **Проверьте логи**:
   ```bash
   docker-compose logs
   ```

2. **Проверьте .env файл**:
   ```bash
   docker-compose exec wfrp-bot cat /app/.env
   ```

3. **Проверьте права доступа**:
   ```bash
   docker-compose exec wfrp-bot ls -la /app/
   ```

4. **Проверьте конфликты портов**:
   ```bash
   docker-compose config
   ```

### Общие проблемы:

**Проблема**: "Permission denied" при доступе к файлам
**Решение**: Проверьте права на папки и права пользователя в Dockerfile

**Проблема**: Контейнер падает сразу после запуска
**Решение**: Проверьте .env файл и логи: `docker-compose logs --tail=100`

**Проблема**: Minimax API ошибки
**Решение**: Проверьте MINIMAX_API_KEY и доступ к API

---

## 📊 Мониторинг и производительность

### Просмотр ресурсов:

```bash
# Статус контейнера с ресурсами
docker stats wfrp-bot

# Информация о контейнере
docker inspect wfrp-bot
```

### Ограничения ресурсов (уже настроены):

```yaml
deploy:
  resources:
    limits:
      cpus: '1.0'
      memory: 512M
    reservations:
      cpus: '0.25'
      memory: 128M
```

---

## 🔄 Обновление бота

```bash
# 1. Остановить контейнер
docker-compose stop

# 2. Обновить код
git pull
# или скопировать новые файлы

# 3. Пересобрать и запустить
docker-compose up -d --build

# 4. Проверить логи
docker-compose logs -f
```

---

## 🗑️ Очистка

```bash
# Остановить и удалить контейнер
docker-compose down

# Удалить данные (история, персонажи)
docker-compose down -v

# Очистить все неиспользуемые образы
docker image prune -a
```

---

## 🎯 Следующие шаги после успешного запуска

1. ✅ Тестирование базовых команд через Telegram
2. ✅ Проверка логов на наличие ошибок
3. ✅ Тестирование сессий и кампаний
4. ✅ Проверка сохранения данных
5. ✅ Тестирование Minimax API
6. ✅ Настройка rate limiting и безопасность

---

## 📞 Техническая поддержка

Если возникнут проблемы:
1. Соберите логи: `docker-compose logs > logs.txt`
2. Проверьте .env файл
3. Проверьте конфигурацию: `docker-compose config`
4. Проверьте переменные окружения: `docker-compose exec wfrp-bot env`
