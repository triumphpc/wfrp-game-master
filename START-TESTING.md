# 🚀 Инструкция по запуску и тестированию

## Моментально: Запуск (3 минуты)

### 1. Подготовка

```bash
# Проверка Docker
docker --version
docker-compose --version

# Создание .env файла
cp .env.minimax.example .env
```

### 2. Конфигурация (.env)

Отредактируйте `.env` файл:

```env
# Telegram (обязательно)
TELEGRAM_BOT_TOKEN=ваш_токен_из_BotFather
TELEGRAM_GROUP_ID=-1001234567890

# Minimax (обязательно)
MINIMAX_API_KEY=ваш_api_key_с_minimax
```

**Как получить:**
- **Токен**: @BotFather → /newbot
- **Group ID**: @userinfobot → перешлите бота в группу
- **API ключ**: https://api.minimax.chat/

### 3. Запуск

```bash
# Быстрый старт
./quick-start.sh

# Или вручную
docker-compose up -d
```

### 4. Проверка

```bash
# Статус контейнера
docker-compose ps

# Логи
docker-compose logs --tail=50
```

**Ожидаемый результат:**
```
[INFO] WFRP Game Master Bot
[INFO] Starting bot...
[INFO] Config loaded. Provider: minimax, Group ID: -100...
```

---

## 🧪 Тестирование (15 минут)

### Автоматическое тестирование

```bash
# Запустить все тесты
./test-docker.sh
```

**Тесты включают:**
- ✅ Проверка предусловий
- ✅ Билд контейнера
- ✅ Запуск контейнера
- ✅ Проверка логов
- ✅ Проверка монтирования
- ✅ Проверка переменных окружения
- ✅ Проверка ресурсов

### Ручное тестирование

#### Тест 1: Проверка логов
```bash
# Запустить мониторинг
docker-compose logs -f

# В другом терминале проверить статус
docker-compose ps
```

#### Тест 2: Проверка команд

Откройте Telegram и попробуйте:
- `/start` - начать игру
- `/help` - справка
- `/status` - статус

**Проверить логи на наличие ошибок:**
```bash
docker-compose logs --tail=50 | grep -i error
```

#### Тест 3: Проверка данных

```bash
# Проверить историю
ls -la history/

# Проверить персонажей
ls -la characters/
```

#### Тест 4: Проверка Minimax

```bash
# Мониторинг логов
docker-compose logs -f

# Отправить сообщение в Telegram
# Проверить логи на наличие Minimax запросов
```

**Ожидаемые логи:**
```
[INFO] Making request to minimax provider
[INFO] Request to minimax succeeded (duration: 2.3s)
```

---

## 📊 Мониторинг во время тестирования

### Основные команды

```bash
# Статус
docker-compose ps

# Логи в реальном времени
docker-compose logs -f wfrp-bot

# Ресурсы
docker stats wfrp-bot

# Проверка здоровья
docker-compose exec wfrp-bot pgrep -f wfrp-bot
```

### Критические показатели

**Нормальная работа:**
- Память: 100-300MB
- CPU: 0.1-0.5%
- Время запуска: < 30 секунд

**Время ответа Minimax:**
- 2-5 секунд (типично)
- До 10 секунд (зависит от сети)

---

## 🎯 Следующие шаги после успешного запуска

### Список задач:
1. ✅ Бот запущен и работает
2. ✅ Команды отвечают корректно
3. ✅ Minimax API работает
4. ✅ Данные сохраняются
5. ✅ Ресурсы в пределах нормы

### Дальнейшее тестирование:
1. 📖 Прочитать TESTING.md (10+ тестов)
2. 🎮 Провести полный цикл тестов
3. 📊 Проверить обработку ошибок
4. ⚡ Проверить производительность
5. 🔒 Проверить безопасность

---

## 🐛 Решение проблем

### Проблема: Контейнер не запускается

```bash
# Проверить логи
docker-compose logs

# Проверить .env
cat .env

# Проверить конфигурацию
docker-compose config

# Перезапустить
docker-compose restart
```

### Проблема: Ошибки Minimax

```bash
# Проверить API ключ
docker-compose exec wfrp-bot env | grep MINIMAX

# Проверить доступность API
curl -I https://api.minimax.chat/v1
```

### Проблема: Данные не сохраняются

```bash
# Проверить монтирование
docker-compose exec wfrp-bot ls -la /app/history/

# Проверить права
docker-compose exec wfrp-bot df -h
```

---

## 💡 Быстрые команды (Makefile)

```bash
make help          # Все команды
make up            # Запуск
make down          # Остановка
make logs          # Логи
make status        # Статус
make restart       # Перезапуск
make test          # Тестирование
make exec          # Вход в контейнер
```

---

## ✅ Критерии успешного запуска

### Минимальные требования:
- ✅ Контейнер запускается без ошибок
- ✅ Логи показывают "Config loaded"
- ✅ Бот отвечает на /start, /help, /status
- ✅ Минimax API работает корректно
- ✅ Данные сохраняются в history/ и characters/

### Оптимальные показатели:
- ✅ Память: < 300MB
- ✅ CPU: < 0.5%
- ✅ Время запуска: < 30 секунд
- ✅ Время ответа: < 5 секунд

---

## 🎉 Готово!

После успешного запуска:
1. ✅ Наслаждайтесь игрой в Telegram!
2. ✅ Тестируйте все функции бота
3. ✅ Проверяйте сохранение данных
4. ✅ Следите за логами

**Удачи! 🚀**

---

## 📚 Дополнительная документация

- **Docker-Setup.md** - полное руководство
- **TESTING.md** - план тестирования
- **DOCKER-READY.md** - краткое руководство
- **DOCKER-COMPLETION-REPORT.md** - статус проекта

**Или просто:**
```bash
make help          # Все команды
./quick-start.sh   # Быстрый старт
./test-docker.sh   # Тестирование
```
